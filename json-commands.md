# שאלות JSON: load / loads / dump / dumps

כל השאלות במאגר שעוסקות בפקודות `json.load` / `loads` / `dump` / `dumps` (3 שאלות).

> **כלל הזהב:** ה-`s` בסוף = **String** (מחרוזת). `loads`/`dumps` → מחרוזת, `load`/`dump` → קובץ. `load*` = קריאה (JSON→פייתון), `dump*` = כתיבה (פייתון→JSON).

---

## 24B-B-Q8 — הרכשת נתונים  ·  2024 סמסטר ב׳ מועד ב׳

**איזו פקודה תמיר json למבנה נתונים של dictionary?**

- **א) הפקודה loads  ✅**
- ב) הפקודה load
- ג) הפקודה dumps
- ד) הפקודה dump

**הסבר:** json.loads ממיר מחרוזת JSON למילון פייתון (dump/dumps עושים את ההמרה ההפוכה, load קורא מקובץ).

---

## 26A-A-Q1 — הרכשת נתונים  ·  2026 סמסטר א׳ מועד א׳

**מה תפקידה של הפקודה json.dumps()?**

- **א) להמיר אובייקט פייתון למחרוזת JSON  ✅**
- ב) לקרוא קובץ JSON מהאינטרנט
- ג) לשלוח טופס לאתר
- ד) לפרוס HTML

**הסבר:** json.dumps() מסריאלת (ממירה) אובייקט פייתון כגון dict למחרוזת בפורמט JSON.

---

## 26A-B-Q3 — הרכשת נתונים  ·  2026 סמסטר א׳ מועד ב׳

**איזו פקודת JSON אחראית על העברה של Dictionary למסמך טקסט?**

- **א) dump  ✅**
- ב) write
- ג) put
- ד) loads

**הסבר:** הפקודה json.dump כותבת מבנה כמו Dictionary לקובץ/מסמך טקסט, בעוד loads קוראת מטקסט.

---
